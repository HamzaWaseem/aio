<%= form_with(model: car_selling, local: true) do |form| %>
  <% if car_selling.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(car_selling.errors.count, "error") %> prohibited this car_selling from being saved:</h2>

      <ul>
      <% car_selling.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :rates %>
    <%= form.text_area :rates %>
  </div>

  <div class="field">
    <%= form.label :details %>
    <%= form.text_area :details %>
  </div>

  <div class="field">
    <%= form.label :published_at %>
    <%= form.datetime_select :published_at %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
